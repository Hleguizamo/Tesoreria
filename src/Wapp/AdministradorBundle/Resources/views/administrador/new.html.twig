{% extends 'base.html.twig' %}

{% block body %}
    <div class="downloadpost" style="font-size:24px;">Crear nuevo administrador</div>	

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear Administrador" class="{{ button_css|default("btn btn-primary") }}" />
    {{ form_end(form) }}
	<div>&nbsp;</div>
    <div>
        <a href="{{ path('administrador_index') }}" class="{{ button_css|default("btn btn-link") }}">
		<i class="fa fa-long-arrow-left"></i> Volver
		</a>
	</div>
	
{% endblock %}
